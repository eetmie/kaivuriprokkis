# DEPRECATED - This file has been split into two separate configs:
#
#   hardware_config.yaml  - Hardware/driver settings (rates, imu, adc, pwm, safety)
#                           Used by: HardwareInterface
#
#   control_config.yaml   - Control algorithm settings (pid, controller, velocity limits)
#                           Used by: ExcavatorController
#
# This file is kept for backwards compatibility but is no longer loaded by default.
# Please migrate your settings to the appropriate config file above.
