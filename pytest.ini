[pytest]
testpaths = tests
python_files = test_*.py
markers =
    integration: tests that exercise multiple modules together
    slow: tests that are useful but slower than the normal unit suite
    hardware: tests that touch or emulate hardware-facing behavior
